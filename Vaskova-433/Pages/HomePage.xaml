<Page x:Class="Vaskova_433.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Vaskova_433.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">

    <Grid>
        <DataGrid x:Name="LaLaLa" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding date}" Width="1*"/>
                <DataGridTextColumn Header="Класс" Binding="{Binding classTrainess}" Width="0.75*"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding firstName}" Width="*"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding lastName}" Width="*"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding middleName}" Width="*"/>
                <DataGridTextColumn Header="Запрос" Binding="{Binding inquiry}" Width="2*"/>
                <DataGridTextColumn Header="Статус выполнения" Binding="{Binding status}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
